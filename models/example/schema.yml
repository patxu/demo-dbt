version: 2

models:
  - name: patxu_testing
    description: "Test dbt schema"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

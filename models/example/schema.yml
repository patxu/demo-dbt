
version: 2

models:
    - name: all_users
      description: "All users in available in the data warehouse. Pulling data from Sales, Marketing, Support, and Product."
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
